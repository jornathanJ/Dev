<?xml version="1.0" encoding="UTF-8"?>
<configuration>
		
	<!-- 외부 설정파일을 사용할 수 있다. -->
	<property resource="resource.properties"/>

	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<layout class="ch.qos.logback.classic.PatternLayout">
			<Pattern>%d{HH:mm} %-5level %logger{36} - %msg%n</Pattern>
		</layout>
	</appender>
	<appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${APP_HOME}/basic-statistics.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- 파일이 하루에 한개씩 생성된다 -->
			<fileNamePattern>basic-statistics.%d{yyyy-MM-dd}.log</fileNamePattern>
			
			<!-- maxHIstory 설정은 위 부분에 롤링 정책에 따라 적용되 된다고 보면된다.
			위 설정데로 라면 30일이 지난 파일은 삭제가 된다.-->
			<maxHistory>30</maxHistory>
		</rollingPolicy>
		
		<encoder>
			<pattern>%-4relative [%thread] %-5level %logger{35} - %msg%n</pattern>
		</encoder>
	</appender>
	<logger name="com.bistel.algo.pdm" level="DEBUG"/>
<!-- 	<logger name="com.bistel.core.stat.app.model.mspc" level="DEBUG"/>
 -->

	<root level="DEBUG">
		<appender-ref ref="FILE" />
<!-- 		<appender-ref ref="STDOUT"/> -->
	</root>
</configuration>
